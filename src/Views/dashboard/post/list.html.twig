{% extends 'dashboard/base_layout.html.twig' %}
{% block stylsheets %}
    {{ parent() }}

{% endblock stylsheets %}
{% block body %}

    <h3 class="text-dark mb-4">Articles</h3>
    <div class="card shadow">
        <div class="card-header py-3 d-flex justify-content-between align-items-center" >
            <p class="text-primary m-0 font-weight-bold">Liste des articles</p>
            <a href="/intranet/post/create" class="btn btn-primary"><span class="d-md-none"><i class="fa fa-plus-circle"></i></span> <span class="d-none d-md-block">Créer un article</span></a>
        </div>
        <div class="card-body pt-0">
            <div class="table-responsive table" id="dataTable" role="grid" aria-describedby="dataTable_info">
                <table class="table my-0" id="dataTable">
                    <thead>
                    <tr>
                        <th>#</th>
                        <th>Titre</th>
                        <th>Auteur</th>
                        <th><strong>Date de modification</strong></th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% if posts %}
                        {% for post in posts %}
                            <tr>
                                <td>{{ loop.index }}</td>
                                <td>{{ post.title }}</td>
                                <td><img class="rounded-circle mr-2" width="30" height="30"
                                         src="https://via.placeholder.com/30">{% if post.author is not null %} {{ post.author.username }} {% else %}
                                         Pas d'auteur {% endif %}</td>
                                <td>{{ post.updatedAt|date('d-m-Y à H:i:s') }}</td>
                                <td>
                                    <a class="btn btn-light" role="button" href="/intranet/post/{{ post.id }}/edit"><i
                                                class="fa fa-cog mr-2"></i>Gérer</a>
                                </td>
                            </tr>
                        {% endfor %}
                    {% else %}
                        <tr>
                            <td>
                                <p>Commencez à créer un dès maintenant</p>
                            </td>
                        </tr>
                    {% endif %}
                    </tbody>
                    <tfoot>
                    <tr>
                        <td><strong>#</strong></td>
                        <td><strong>Titre</strong></td>
                        <td><strong>Auteur</strong></td>
                        <td><strong>Date de modification</strong></td>
                        <td><strong>Action</strong></td>
                    </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
{% endblock body %}

{% block javascripts %}
    {{ parent() }}

{% endblock javascripts %}